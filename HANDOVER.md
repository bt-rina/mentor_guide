# 멘토 가이드 사이트 인수인계 가이드

> 이 사이트를 **다른 사람의 GitHub · Vercel 계정으로 완전히 넘기는** 방법입니다.
> 개발을 몰라도 따라올 수 있게 썼습니다.

---

## 0. 먼저 알아둘 것 — 넘겨야 하는 건 "두 개"입니다

| | 무엇 | 지금 상태 |
|---|---|---|
| ① | **GitHub 저장소** — 파일 원본이 보관된 곳 | `github.com/bt-rina/mentor_guide` (공개) |
| ② | **Vercel 프로젝트** — 그 파일을 웹사이트로 띄워주는 곳 | Vercel이 위 저장소를 바라보며 자동 배포 중 |

둘의 관계는 이렇습니다.

```
GitHub 저장소  ──(파일이 바뀌면 자동으로 알림)──▶  Vercel  ──▶  실제 웹사이트 주소
```

**중요**: Vercel은 저장소를 "복사해 두는" 게 아니라 **매번 GitHub에서 읽어옵니다.**
그래서 GitHub 쪽만 넘기고 끝내면, 인수자는 사이트를 고칠 수는 있어도
**배포되는 웹사이트는 여전히 내 Vercel 계정에 남아 있게 됩니다.** 둘 다 옮겨야 합니다.

---

## 1. 어떤 방법이 좋을까 — 3가지 비교

| 방법 | 어떻게 | 수정 이력 | 난이도 | 추천 |
|---|---|---|---|---|
| **A. 저장소 소유권 이전** | GitHub 설정에서 저장소 자체를 인수자 계정으로 넘김 | ✅ 그대로 유지 | ⭐ 가장 쉬움 (클릭 몇 번) | ✅ **이걸 추천** |
| **B. 협업자로 초대** | 인수자를 내 저장소에 초대 → 인수자가 자기 저장소로 복사 | ✅ 유지 | ⭐⭐ 인수자가 명령어를 좀 써야 함 | 내 사본을 꼭 남겨야 할 때 |
| **C. zip 파일 전달** | 폴더를 압축해서 전달 | ❌ **사라짐** | ⭐ 쉬움 | 권장하지 않음 |

### 왜 zip은 권장하지 않나요?

되기는 됩니다. 사이트는 똑같이 뜹니다. 다만 두 가지를 잃습니다.

- **수정 이력**: "언제 누가 무엇을 왜 바꿨는지" 기록이 통째로 사라집니다.
  나중에 "이 문구 왜 이렇게 바뀌었지?"를 되짚을 수 없습니다.
- **되돌리기**: 뭔가 잘못 고쳤을 때 이전 상태로 돌아가는 안전장치가 없어집니다.

**A 방법은 zip보다 오히려 더 쉽습니다.** 버튼 몇 번이면 끝나고, 이력도 그대로 갑니다.
"개발자스러우면서 동시에 쉬운 방법"을 찾으신다면 A가 정답입니다.

### 그럼 "내 저장소 권한만 주면 알아서 옮기지 않을까?"

가능은 하지만 **인수자에게 일을 떠넘기는 방식**입니다.
인수자가 직접 복사(clone)해서 자기 저장소로 밀어 넣는(push) 작업을 해야 하는데,
이건 터미널 명령어를 쓸 줄 알아야 합니다. 인수자가 개발자가 아니라면 막힙니다.
**A는 인수자가 "수락" 버튼만 누르면 끝납니다.**

---

## 2. 추천 방법 (A) — 단계별

### 🔸 0단계. 넘기기 전에 반드시 — 최신 내용 저장(커밋)하기

> ⚠️ **지금 로컬 폴더의 개편 내용이 아직 GitHub에 올라가 있지 않습니다.**
> 이 상태로 저장소를 넘기면 인수자는 **예전 버전**을 받게 됩니다.

터미널에서 이 폴더로 이동한 뒤:

```bash
git add .
git commit -m "가이드 개편: 단계별 페이지 분리, 통합 검색, 필수 미션 신설"
git push
```

`github.com/bt-rina/mentor_guide` 에 들어가서 파일이 최신인지 눈으로 확인하세요.

---

### 🔸 1단계. GitHub 저장소를 인수자에게 넘기기

준비물: **인수자의 GitHub 아이디** (없으면 [github.com](https://github.com) 에서 무료 가입)

1. `github.com/bt-rina/mentor_guide` 접속
2. 상단 **Settings** (톱니바퀴 아이콘) 클릭
3. 맨 아래로 스크롤 → 빨간색 **Danger Zone** 영역
4. **Transfer ownership** → **Transfer** 버튼 클릭
5. 팝업에 입력
   - 새 소유자: **인수자의 GitHub 아이디**
   - 확인란: 저장소 이름 `mentor_guide` 를 그대로 타이핑
6. **I understand, transfer this repository** 클릭

이제 인수자에게 GitHub 알림과 이메일이 갑니다.
**인수자가 수락하면 이전 완료**입니다. (보통 몇 초)

**이전 후 이렇게 됩니다**
- 주소가 `github.com/인수자아이디/mentor_guide` 로 바뀝니다.
- 옛날 주소로 들어가도 **새 주소로 자동 연결**됩니다. 링크가 깨지지 않습니다.
- 수정 이력·설정이 모두 따라갑니다.
- **나는 더 이상 이 저장소에 접근할 수 없습니다.** 계속 보고 싶다면
  인수자에게 협업자(Collaborator)로 초대해 달라고 하면 됩니다.

> 💡 **내 사본을 남기고 싶다면**: 이전하기 **전에** 저장소 첫 화면 오른쪽 위
> **Fork** 버튼을 눌러 복사본을 하나 만들어 두세요. 그 다음 원본을 이전하면 됩니다.

---

### 🔸 2단계. Vercel에 새로 연결하기 (인수자가 진행)

Vercel은 계정끼리 프로젝트를 옮기는 게 번거롭습니다.
**그냥 인수자 계정에서 새로 가져오는 게 훨씬 빠릅니다.** (이 사이트는 설정이랄 게 없어서 1분이면 끝납니다)

인수자가 할 일:

1. [vercel.com](https://vercel.com) 접속 → **Continue with GitHub** 으로 가입/로그인
   (본인 GitHub 계정으로 로그인해야 저장소가 보입니다)
2. **Add New...** → **Project**
3. 목록에서 `mentor_guide` 찾아 **Import**
   - 안 보이면 **Adjust GitHub App Permissions** 를 눌러 이 저장소에 접근을 허용
4. 설정 화면이 나오면 **아무것도 건드리지 말고** 그대로 **Deploy**
   - Framework Preset: `Other` (자동으로 잡힙니다)
   - Build Command / Output Directory: **비워 두기** — HTML 파일만 있는 사이트라 빌드 과정이 없습니다
5. 1~2분 뒤 배포 완료 → `프로젝트이름.vercel.app` 주소가 생깁니다

이제부터 **인수자가 GitHub에서 파일을 고치면 Vercel이 자동으로 사이트를 갱신**합니다.

---

### 🔸 3단계. 내 Vercel 프로젝트 정리 (내가 진행)

인수자 사이트가 정상으로 뜨는 걸 **확인한 다음에** 하세요.

1. [vercel.com](https://vercel.com) 내 계정 → 기존 프로젝트 선택
2. **Settings** → 맨 아래 **Delete Project**

안 지우면 **같은 사이트가 두 개 살아있게 됩니다.** 나중에 어느 쪽이 진짜인지 헷갈리고,
멘토님들이 옛 주소를 계속 쓰면 인수자가 고친 내용이 반영되지 않습니다.

> 바로 지우기 부담스러우면, 삭제 대신 Settings → Git 에서 **Disconnect**(연결 끊기)만 해도
> 자동 갱신은 멈춥니다. 다만 옛 사이트 주소는 계속 살아 있으니 결국 삭제를 권합니다.

---

## 3. 웹사이트 주소가 바뀝니다 — 이건 꼭 챙기세요

이전 후 인수자 쪽에 **새 주소**가 생깁니다.

**같은 주소를 그대로 쓰고 싶다면** (예: `mentor-guide.vercel.app`)

- `.vercel.app` 주소는 전 세계에서 하나뿐이라, **내가 먼저 비워줘야** 인수자가 쓸 수 있습니다.
- 순서: 내가 3단계(삭제) 먼저 → 인수자가 Vercel 프로젝트 Settings → **Project Name** 을 그 이름으로 변경

**회사 도메인을 연결해 쓰고 있었다면** (예: `guide.feedbluetiger.com`)

- 이게 제일 깔끔합니다. 도메인만 옮기면 멘토님들이 쓰던 링크가 그대로 살아납니다.
- 순서: 내 Vercel 프로젝트에서 도메인 **Remove** → 인수자 프로젝트 Settings → Domains 에서 **Add**
- 도메인 관리처(가비아·Cloudflare 등)에서 DNS를 바꿔야 할 수도 있습니다. 이건 인수자와 함께 진행하세요.

**주소가 바뀐다면** 멘토님들께 공지하고, 기존 공유 링크(디스코드 공지, 알림톡 등)를 모두 교체해야 합니다.

---

## 4. 넘기기 전 최종 체크리스트

- [ ] **최신 내용을 커밋·푸시했다** (0단계) — 안 하면 옛 버전이 넘어갑니다
- [ ] 내 사본이 필요하면 **Fork** 로 복사본을 만들어 뒀다
- [ ] 인수자의 **GitHub 아이디**를 확인했다
- [ ] 현재 웹사이트 주소를 적어 뒀다 (나중에 새 주소와 비교)
- [ ] **공개 저장소라는 점을 확인했다** — 아래 주의사항 참고
- [ ] 인수자에게 이 문서(`HANDOVER.md`)를 함께 전달했다

### ⚠️ 공개 저장소 관련 주의

`mentor_guide` 는 **공개(public)** 저장소입니다. 즉 누구나 파일을 볼 수 있습니다.
이 폴더에는 아래 파일들이 있습니다.

- `feedback_video_compressed.mp4`, `qna_video_compressed.mp4` — **실제 멘토링 녹화 영상**
- `communication_image1.png`, `communication_image2.png` — **수강생과의 대화 캡처**

커밋해서 올리면 **인터넷에 공개**됩니다. 한 번 올라가면 나중에 지워도
검색엔진 캐시나 다른 사람의 복사본에 남을 수 있습니다.

**권장**: 인수인계 전에 저장소를 **비공개(Private)** 로 바꾸세요.
`Settings → Danger Zone → Change repository visibility → Make private`
비공개여도 Vercel 배포와 웹사이트 공개에는 **아무 지장이 없습니다.**

---

## 5. 인수인계 후 — 인수자가 알아야 할 것

### 내용을 수정하려면

1. GitHub에서 파일(예: `step-1-basics.html`) 클릭 → 연필 아이콘 → 수정 → **Commit changes**
2. 1~2분 뒤 웹사이트에 자동 반영

디자인·구조를 바꾸려면 같은 폴더의 **`DESIGN_SYSTEM.md`** 를 먼저 읽어보세요.
색상·컴포넌트·페이지 추가 방법이 정리되어 있습니다.

### 파일 구성

```
index.html                          메인 (목차 · 링크 모음 · 체크리스트 · FAQ)
step-1-basics.html                  STEP 1 기본 사항
step-2-preparation.html             STEP 2 사전 준비사항
step-3-missions.html                STEP 3 멘토 필수 미션
step-4-lecture-assignment.html      STEP 4 강의 숙지 및 과제 점검
step-5-mentoring.html               STEP 5 멘토링 진행
step-6-encouragement.html           STEP 6 수강생 참여 독려
mentoring-examples.html             ↳ 멘토링 예시 자료

style.css                           전체 디자인
main.js                             검색 · 메뉴 · 체크리스트 동작
DESIGN_SYSTEM.md                    디자인/구조 규칙 문서
*.png, *.mp4, OBS_profile.zip       이미지 · 영상 · 첨부파일
```

### 주의: 검색 기능은 웹 서버에서만 동작합니다

파일을 더블클릭해서 열면 검색이 작동하지 않습니다. (브라우저 보안 정책 때문)
Vercel에 올라간 주소로 접속하면 정상입니다.

---

## 6. 용어 풀이

| 용어 | 뜻 |
|---|---|
| **저장소 (Repository)** | 파일들이 이력과 함께 보관되는 GitHub의 폴더 |
| **커밋 (Commit)** | 변경 내용을 "이 시점"으로 저장하는 것. 되돌리기의 기준점 |
| **푸시 (Push)** | 내 컴퓨터의 변경 내용을 GitHub로 올리는 것 |
| **포크 (Fork)** | 저장소를 내 계정으로 복사해 오는 것 |
| **배포 (Deploy)** | 파일을 실제 웹사이트로 띄우는 것 |
| **Vercel** | 웹사이트를 자동으로 띄워주는 무료 서비스 |

---

## 7. 막혔을 때

| 증상 | 원인과 해결 |
|---|---|
| 이전 버튼이 눌리지 않음 | 인수자 아이디 오타 · 저장소 이름 확인란 오타 |
| 인수자 Vercel에 저장소가 안 보임 | **Adjust GitHub App Permissions** 에서 해당 저장소 접근 허용 |
| 배포는 됐는데 화면이 깨짐 | Output Directory 를 건드렸을 가능성. **비워 두고** 다시 Deploy |
| 이미지·영상이 안 보임 | 해당 파일이 커밋되지 않았을 수 있음. GitHub에서 파일 존재 확인 |
| 검색이 안 됨 | 파일을 직접 연 경우입니다. Vercel 주소로 접속하세요 |
